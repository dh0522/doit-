#include <stdio.h>

int main(){
	int a,b;
	printf("a의 값: ");
	scanf("%d",&a);
	
	while(1)
	{
		printf("b의 값: ");
		scanf("%d",&b);
		if(b<=a)	printf("a보다 큰 값을 입력하세요!\n");
		else	break; 
	}
	printf("b-a의 값은 %d입니다.\n",b-a);
}